version: 2.1

orbs:
  win: circleci/windows@2.2.0

jobs:
  test_and_build:
    description: "Setup the application and run the tests"
    executor: win/default
    working_directory: ~/repo
    steps:
      - checkout
      - restore_cache:
          keys:
            - dotnet-packages-v1-{{ checksum "api/api.csproj" }}
      - run:
          name: "Install project dependencies"
          command: dotnet restore
      - save_cache:
          paths:
            - C:\Users\circleci\.nuget\packages
          key: dotnet-packages-v1-{{ checksum "api/api.csproj" }}

      - run:
          name: "Run application tests"
          command: dotnet test --filter "api.test"

      - run:
          name: "Build the application"
          command: dotnet build

      - persist_to_workspace:
          root: ~/repo
          paths: .

      - run:
          name: "Install Lambda tools"
          command: dotnet tool install --global Amazon.Lambda.Tools --version 3.1.1

      - run:
          name: "Update Lambda tools"
          command: dotnet tool update -g Amazon.Lambda.Tools

      - run:
          name: Deploy application
          command: dotnet lambda package --configuration release --framework netcoreapp3.1 --output-package ./repo/bin/release/netcoreapp3.1/deploy-package.zip


workflows:
  build-and-test:
    jobs:
      - test_and_build
